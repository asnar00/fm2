# transcript: saturday
*session `ca6175f3-aaa3-426c-9021-c9d6342188c7`, exported by tools/export_transcript.py — do not edit; anchors `#pN` are stable*

### p1
*2026-09-01 14:07*

> hi claude! fresh session today - could you please take a look around the project and tell me what you think?

Morning! Fresh session — let me read the handover and current state first.

The handover is dated 2026-08-25/26 but the git log shows newer work (recordings-as-posts, whisper on the mini, project select/delete). Let me catch up on what's happened since.

Today is 1 September — the prototype date from the campaign brief. Let me check the live system and read the required docs (agents.md, the composed skillset).

Had a good look around. Here's where things stand:

**Everything is healthy.** The live server answers with build 407, the repo is at 408 commits (so exactly one unshipped commit's worth of counting — nothing is actually waiting; the tree is clean, nothing staged or dirty). The whisper model and ONNX runtime are properly in `site/stt/` now, so the miss from the 28th (six builds shipped without the model) is fixed and stays fixed — deploy.sh refuses without it. The ask store has **zero pending asks** — everything filed from the field has been shipped and stamped. No stray worktrees left either.

**Where the story is:** The last session (28 Aug, on this mini) landed three things — fm2 moved here permanently, projects got select/delete, and recordings became posts with their transcripts flowing into the boot index. The composed skillset now carries five agent-instruction nodes (taste, did-you-mean, attention, glyphs, anticipation) and I've read it.

**Today is 1 September** — the prototype date from the campaign brief. The handover's "FOR ASH" list (update the phone, invite Tara, make the "sevenoaks 2029" project, decide on CARTO vs OSM tiles and project-scoped visibility) may already be done or may be today's agenda — you'd know better than the docs do.

**One note:** handover.md still says "written 2026-08-25" and headlines build 398, but the mini session on the 28th shipped through 407 — it was partially updated (the tooling section knows about the mini) but the headline is two sessions stale. Worth a rewrite at some point.

**The named next work**, from the handover, in its own order: the `/keep` caret race (typing right after a repaint lands a character early — reproducible with the invite-rig pattern, known bug, "fix first"), then tree-owned smoke steps, project membership as a visibility cue, and the var-per-card/blob-path efficiency work.

My honest take: the system is in the best shape I've seen it described — green gate, empty ask queue, clean tree, model artifacts in place. If you're seeing Tara today, the caret-race bug is the one thing I'd want fixed before a live user types on a phone, since it corrupts text invisibly. Want me to start on that, or is something else first?

### p2
*2026-09-01 14:21*

> I demoed to Tara - she liked it, and our first actual canvassing session is on Saturday! So we have some work to do to get ready. In no particular order: 1] show constituency and ward boundaries on the map; 2] clean up the "make posts" interface to include video, audio, photo+type, transcription 3] make a quicker QR-code based invite workflow that we can use instantly during canvassing 4] AI interrogation / report generation.

Brilliant — congratulations! Saturday it is. Let me set up: first the transcript export so these four asks get their provenance anchors, then I'll lay out the plan.

All four asks anchor at `transcripts/2026-09-01-saturday.md#p2`. Now a quick survey of the ground each one builds on — the map node, the posts/record toolbar, and the invite flow.

The tree survey is done — I know where each ask lands. Let me re-read hybrid.md's briefing discipline before writing worker briefs.

Before writing briefs, the discipline says read `misses.md` in full, and I want the real seam names for the map and invite nodes.

One check before briefing the posts worker: how recordings store their audio today — that decides whether video rides an existing seam or is a foundation ask.

Three workers are building in parallel worktrees. While they run — ask 4 is the one where more than one reading survives, and it also needs a decision that's genuinely yours:

The server is Rust — no official SDK — so the worker will call the API over raw HTTP. Let me pull the exact request shape.

Now the fourth brief — the reports tool.

All four workers are now running. One practical check while they build — whether the mini already has an API key the server could use:

All four workstreams are now in motion. Here's the state of play:

**Building now, in four parallel worktrees** (Opus workers, each following the full node discipline; I review, integrate, and deploy each one serially as it's proven):

1. **Boundaries** — a `boundaries` node under the map: Sevenoaks constituency outline plus district ward lines from ONS open data, committed as node assets (no runtime third-party fetch, learned from the whisper-model miss), drawn quiet and dusty per `/taste`.
2. **Make-posts cleanup** — one clean capture flow in the posts tool: photo, video, audio, typed, photo+words, with speech becoming the post's words. Video bytes go the exchange blob route like audio — never onto the card (the 64KB wire cap is in the brief). Note: the posts node is at the 6-child cap, so this one may involve a regroup with a `--chains` proof.
3. **QR invite** — the canvasser taps once, shows a full-screen QR; the person scans, types their own name and number on their own phone, and is on the guest list under your name — you never type at the door. Multi-use token, expiring, revocable, with the abuse cases reasoned through.
4. **Reports tool** — per your answers: a support-and-above tool, multiple reports, one query each, refreshable in place, output as a light print-styled PDF with a map page, generated server-side via the Claude API (Opus 5) and rendered with headless Chrome. Sharing is via the phone's share sheet on the PDF — a public link would be an outward-facing ruling, so I left a seam rather than building one.

**One thing I'll need from you before reports can go live**: an Anthropic API key on the mini for the miso server. The natural home is `~/.agent-config.json`, where the Vonage credentials already live — the worker is building it so everything works keyless in the rig, and the tool just quietly says so until a key appears. I'll confirm the exact field name when the node lands.

I'll check in on the workers on the usual cadence and review/ship each as it comes back. The transcript is exported and all four asks carry the `#p2` anchor. Reports and boundaries were briefed to stay independent, so whichever lands first ships first — and the report map grows ward overlays once boundaries are in.
