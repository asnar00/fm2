# transcript: fm spec discussion
*session `107bb18f-2441-4411-9d90-87c9bd3dd178`, exported by tools/export_transcript.py — do not edit; anchors `#pN` are stable*

### p1
*2026-08-16 11:17*

> hi claude! welcome back to fm2 - wanna take a look around and get refamiliarised?

Hi! Yes — let me read the handover and get current.

Handover read — build 175 is live, tree is clean. Let me check the live state and re-arm the session tooling per the handover's checklist.

Live build 175 confirmed and the mini is reachable on the LAN. Now re-arming the ask monitor — first a peek at the asks var format so the filter parses it right.

All caught up. Here's the state of play:

**Live system** — build 175 is serving at miso.nøøb.org (confirmed via /version), 110 nodes. The mini is reachable on the LAN and pushing notifications fine (two 201s to Apple's push service in the recent log). Locally, the dev server is running the build-175 binary on 8095 with auto-export active, and headless Chrome is still up on 9222. The tree is clean at commit 181 — the six commits past the deploy are all notes/handover, so nothing is waiting to ship.

**Re-armed the ask monitor** — recreated it in the session scratchpad and it's running in the background. This version manages its own ssh subprocess with a reconnect loop (the lid-sleep lesson from last night), absorbs the current 11 shipped asks silently, and exits the moment a new `asked`/`proposed` event lands — which wakes me automatically, so there's no polling gap.

**Where last session left things**, per the handover:

- **The evening's headline**: seven field asks went phone-to-phone in one sitting (decrement restore, sub-tool-cards, tooltip repair, fresh-catalog, bigger-buttons, miso-button, request-box), plus auto-export — edit a feature's words and every surface follows without a deploy.
- **Doctrine converged**: tunables, the promotion rule ("second ask earns the variable"), and the surface-side rule of two all point at one mechanism — **the context manager / per-user ticks** (rung 3).
- **Named next rungs**: transcript mirroring first (field-confirmed gap — the phone transcribed on-device but the laptop never heard about it, since /mirror moves audio only), then whisper-on-webgpu, the flywheel, and the context manager.
- **Rulings queued for you**: where a tunable is declared, whose scope a repeat ask writes to, what graduates a tool out of the new-tools drawer, and the skillset fragment format.
- **Regroup pressure**: `ask` joined the at-cap list — its next child forces a regroup (miso-button + request-box look like a natural "wording" group).

Ready when you are — transcript mirroring is the handover's rung 0 if you want to start there, or I'm listening for asks in the meantime.

### p2
*2026-08-16 11:20*

> yeah, I think I'd like to first fix the distribution issue with the transcript

Good — rung 0 it is. Let me load the discipline and the relevant nodes first.

Now the code side — how transcripts are stored and how the mirror moves records.
