cards: String = "[]".to_string() (user, last-write, own) js:cards
# the tile renderer's dev mount, off unless the URL asks (?cardtiles=1); it
# never travels, so it is device-scoped like the toolbar's open_tool
tiles: bool = false (device, last-write, own)
