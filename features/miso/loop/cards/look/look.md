# look
*how a card looks: its placement and its marks*

> (transcripts/2026-08-25-accounts.md#p88)
> my hitlist before tara comes in: 1) projects; 2) posts; 3) map view.
> *(the asks whose nodes needed room under `cards`, forcing this regroup)*

## user

Browse the children: where the card's parts sit (`/layout`) and what it says about itself (`/marks`).

## spec

Grouping node, created under the 4–6 children rule: `cards` stood at six (page, layout, marks, guard, browse, exchange) and projects and posts needed a home. `layout` and `marks` are both about how a card *looks*, so they group here; `/kinds` groups the card types beside. With the linker's tie-break (2026-08-25) a grouping node is order-neutral by construction — verified by a `--chains` diff. Contributes no code.

## glossary

(no new terms)

## code description

No implementation files — a grouping node; `order.md` orders the children.
