# kinds
*the card types and their surfaces: projects, posts*

*Created under the 4–6 children rule (fm.md), 2026-08-25, when the hitlist asks (accounts #p87) needed room under `cards`; no prompt of its own.*

## user

Browse the children: making a card of a type (`/new`), projects (`/projects`), posts (`/posts`), reports (`/reports`), and who a post reaches (`/audience`).

## spec

Grouping node for the card types that have a surface of their own. `/new` is the one door they share for making a card. Contributes no code.

## glossary

(no new terms)

## code description

No implementation files — a grouping node; `order.md` orders the children.
