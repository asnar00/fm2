# the launcher row's order, as a JSON list of tool ids. User-scoped: the row
# you arranged is yours and follows you to every device you sign in on.
# Ids the list does not name keep registration order, after the ones it does —
# so a tool that ships tomorrow still appears, at the end.
tool_order: String = "[]".to_string() (user, last-write, own)
