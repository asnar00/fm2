const feature_RequestBox = {};
{
  // the parent built the row already (fragments load in provenance order);
  // one word where a sentence was — the panel-calm taste, applied to prose
  const fm_askText = document.getElementById('askText');
  if (fm_askText) fm_askText.setAttribute('placeholder', 'request');
}
