# quiet
*filing an ask says nothing*

> (asks#1787704347228)
> Remove the "filed - the builder will see it" text
> *(filed from the field on 2026-08-26 by ash)*

## user

Send an ask to the builder and the box simply empties; the ask appears in your requests list, which is the receipt.

## spec

`/ask` answered a filing with a line under the box — "filed — the builder will see it", or "nothing here does that yet — filed for the builder". Ash asked for it gone (`asks#1787704347228`). One reading, so it builds: this node hides `.asknote` in the results box. The input clearing and the new row in the requests list already say what happened, which is `/taste` 8 — silence is the default. Untick and the line returns.

## glossary

(no new terms)

## code description

`quiet.index.css` — one rule.
